{
  "name": "mermaid-maker-monorepo",
  "version": "0.2.0-dev",
  "description": "MermaidMaker monorepo: web editor + MCP server + shared core. MIT licensed.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @akitaroh/mermaid-maker-web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r --parallel test:watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.33.0"
}
